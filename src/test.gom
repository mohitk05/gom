import io;

let five = 5;
let ten = 10;

fn add(x: int, y: int): int {
    return x + y;
}

fn main(): int {
    let result = add(five, ten);
    io.log_star("Start");
    io.log("Sum " + toString(result));
    io.log_star("End");
    return 0;
}

main();